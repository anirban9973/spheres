int eventspercycle = 10;  // # events per sphere per cycle
int N = 10000;                         // number of spheres
double temp = 0.2;    // initial temp., use 0 for zero velocities
double growthrate = 1e-6;            // growth rate
double maxpressure = 1e6;            // max pressure
int equilsteps = 1000                 // # cycles to equilibrate at fixed radius before growth
char* readfile = new                   // if new, creates new packing
char* writefile = config_initial.dat  // after mp2
char* datafile = stats_initial.dat  // data file up to mp2
